[build]
  command = "npm run build"
  publish = "dist"

# SPA: every path loads the app so React Router can render /about, /p/lumina, etc.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
