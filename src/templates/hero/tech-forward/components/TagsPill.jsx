import React from 'react';
import { TAG_ONE, TAG_TWO } from '../constants';

const TagsPill = () => (
  <div className="tf-tags">
    <span>{TAG_ONE}</span>
    <span>{TAG_TWO}</span>
  </div>
);

export default TagsPill;
