import { ROUTES } from '../../../config';
import { SPINNING_BORDER_BUTTON_RECTANGLE_BUTTONS_PROMPT } from './prompt';
import { PREVIEW_STILL } from './constants';

export const meta = {
  id: 'spinning-border-button-rectangle-buttons',
  title: 'Spinning Border Button Rectangle Buttons',
  category: 'Button',
  categorySlug: 'button',
  pricing: 'Free',
  likes: 2594,
  height: 'h-[272px]',
  livePath: ROUTES.SPINNING_BORDER_BUTTON_RECTANGLE_BUTTONS,
  detailPath: '/templates/spinning-border-button-rectangle-buttons',
  fullPrompt: SPINNING_BORDER_BUTTON_RECTANGLE_BUTTONS_PROMPT,
  description:
    'A compact graphite CTA that replaces its static edge with a rotating white border beam on hover.',
  previewImage: PREVIEW_STILL,
  previewGif: PREVIEW_STILL,
};

export default meta;
