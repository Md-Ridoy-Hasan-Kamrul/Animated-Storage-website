import { ROUTES } from '../../../config';
import { FLOATING_DOTS_CTA_RECTANGLE_BUTTONS_PROMPT } from './prompt';
import { PREVIEW_STILL } from './constants';

export const meta = {
  id: 'floating-dots-cta-rectangle-buttons',
  title: 'Floating Dots CTA Rectangle Buttons',
  category: 'Button',
  categorySlug: 'button',
  pricing: 'Free',
  likes: 2531,
  height: 'h-[272px]',
  livePath: ROUTES.FLOATING_DOTS_CTA_RECTANGLE_BUTTONS,
  detailPath: '/templates/floating-dots-cta-rectangle-buttons',
  fullPrompt: FLOATING_DOTS_CTA_RECTANGLE_BUTTONS_PROMPT,
  description:
    'A saturated blue CTA with a glass highlight and a continuous stream of floating light points.',
  previewImage: PREVIEW_STILL,
  previewGif: PREVIEW_STILL,
};

export default meta;
