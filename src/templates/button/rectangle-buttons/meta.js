import { ROUTES } from '../../../config';
import { RECTANGLE_BUTTONS_PROMPT } from './prompt';
import { PREVIEW_STILL } from './constants';

export const meta = {
  id: 'rectangle-buttons',
  title: 'Rectangle Buttons',
  category: 'Button',
  categorySlug: 'button',
  pricing: 'Free',
  likes: 2685,
  height: 'h-[272px]',
  livePath: ROUTES.RECTANGLE_BUTTONS,
  detailPath: '/templates/rectangle-buttons',
  fullPrompt: RECTANGLE_BUTTONS_PROMPT,
  description:
    'A deep charcoal glass rectangle with a softly revolving inner highlight and restrained metallic type.',
  previewImage: PREVIEW_STILL,
  previewGif: PREVIEW_STILL,
};

export default meta;
