import { ROUTES } from '../../../config';
import { LUMEN_CTA_RECTANGLE_BUTTONS_PROMPT } from './prompt';
import { PREVIEW_STILL } from './constants';

export const meta = {
  id: 'lumen-cta-rectangle-buttons',
  title: 'Lumen CTA Rectangle Buttons',
  category: 'Button',
  categorySlug: 'button',
  pricing: 'Free',
  likes: 2718,
  height: 'h-[272px]',
  livePath: ROUTES.LUMEN_CTA_RECTANGLE_BUTTONS,
  detailPath: '/templates/lumen-cta-rectangle-buttons',
  fullPrompt: LUMEN_CTA_RECTANGLE_BUTTONS_PROMPT,
  description:
    'The authored six-stop violet gradient pill with its inner highlight, cast glow, and trailing open ring.',
  previewImage: PREVIEW_STILL,
  previewGif: PREVIEW_STILL,
};

export default meta;
