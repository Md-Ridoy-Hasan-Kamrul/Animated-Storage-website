import { ROUTES } from '../../../config';
import { GRADIENT_CTA_RECTANGLE_BUTTONS_PROMPT } from './prompt';
import { PREVIEW_STILL } from './constants';

export const meta = {
  id: 'gradient-cta-rectangle-buttons',
  title: 'Gradient CTA Rectangle Buttons',
  category: 'Button',
  categorySlug: 'button',
  pricing: 'Free',
  likes: 2642,
  height: 'h-[272px]',
  livePath: ROUTES.GRADIENT_CTA_RECTANGLE_BUTTONS,
  detailPath: '/templates/gradient-cta-rectangle-buttons',
  fullPrompt: GRADIENT_CTA_RECTANGLE_BUTTONS_PROMPT,
  description:
    'A sunlit amber CTA with inset highlights, deep elevation, and a rising hover sheen.',
  previewImage: PREVIEW_STILL,
  previewGif: PREVIEW_STILL,
};

export default meta;
