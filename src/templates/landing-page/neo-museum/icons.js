import { Bone, BookOpen, Dna, Gem, Leaf } from 'lucide-react';

export const PILL_ICONS = Object.freeze({
  Bone,
  Dna,
  Gem,
  Leaf,
  BookOpen,
});

export const CIRCLE_ICONS = Object.freeze([Bone, Dna, Leaf]);
